public class Notes {
    // Servlet is an interface
    // Web component or web technology
    // Web application: An software residing on web server and delivered through internet using browser
    // Architecture pattern: Client - Server
    // Client (Software waiting for a service)
    // Server (Software provide a service)
    // Overview of a overall view is architecture
    // Design pattern: MVC (MODEL  - VIEW - CONTROLLER)

    // client -> request (url) -> browser -> DNS -> Web server -> browser
    // browser -> web server -> application server (Servlets resides here) -> DB server

}
